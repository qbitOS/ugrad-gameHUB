# Contributing

1. **Games hub and shared web code** — open PRs against **[qbitOS/uvspeed](https://github.com/qbitOS/uvspeed)** (`web/games-ugrad-hub.html`, `web/*.js`).
2. **This repo** — landing copy, `README.md`, `COMPLIANCE.qmd`, and `index.html` link fixes welcome when they stay aligned with upstream Pages URLs.
3. **License** — contributions are under **Apache-2.0** (see root `LICENSE`).
